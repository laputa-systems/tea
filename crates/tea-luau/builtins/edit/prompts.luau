return [[
Workspace mutation

Use `edit` to create, replace, or precisely modify files. It is the only
workspace-mutation tool: use complete content to create or replace a file, and
exact replacements for targeted changes.
]]
