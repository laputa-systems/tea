return [[
Workspace file discovery

Use `find` to efficiently locate workspace files with Tea's bounded,
deterministic glob search. It is faster and more predictable than repeatedly
spawning shell find commands.
]]
