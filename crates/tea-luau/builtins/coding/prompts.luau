return [[
Coding tools

Use `read` to inspect file contents. Use `find` to efficiently locate files.
Use `edit` to create or transactionally modify workspace files. Use `bash` for
everything else, including `ls`, `rg`, `grep`, `find`, git, build commands,
and other shell operations. There are no separate `write`, `grep`, or `ls`
tools.
]]
