return [[
Coding tools

Use `read` to inspect known files. Use `find` to efficiently locate files.
Use `edit` to create, replace, or modify files. Use `bash` for commands, text
search with `rg` or `grep`, git, builds, and everything else. There are no
separate `write`, `grep`, or `ls` tools.
]]
