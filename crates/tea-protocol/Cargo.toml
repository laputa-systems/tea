[package]
name = "tea-protocol"
version = "1.0.0"
edition = "2021"
description = "Stable, runtime-independent protocol contracts for tea"
license = "MIT OR Apache-2.0"

[lib]
path = "src/lib.rs"

[features]
default = []

[dependencies]
miniserde = { workspace = true }
